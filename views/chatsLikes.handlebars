<section>
  {{#each likes}}
    <div class="card">
      <img src="{{this.pet.profile_picture}}" alt="{{this.pet.name}}'s Profile Picture" style="width:100%">
      <button>Start Chatting</button>
    </div>
  {{/each}}
</section>
<link rel="stylesheet" href="/css/profile.css">
